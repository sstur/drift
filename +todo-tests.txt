Routing:
  regex `this.params.$1` and named `this.params.id`
  pre-route, match-route, no-route


parseHeaders

db.query with various number/order params
req.query case-insensitive

Front-End Tests
  Properly report 500 Err
  Conform XHR so tests.js can be used from node and browser
  Shims, JSON, Buffer, MD5 and BodyParser should test via request for cross-platform
  req.headers(h) always returns string

Command-line tests:
  url.parse
  url.resolve
  url.normalize
