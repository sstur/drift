/*global app */
app.cfg({
  //filesystem directory for data (logs, uploads, etc)
  data_dir: 'data/',

  //model persistence
  models: {
    database: 'mysql'
  },

  //database named connections
  mysql: {
    utc_dates: true,
    connections: {
      'default': {server: 'localhost', database: 'database', username: 'username', password: 'password'}
    }
  },

  //remote address in http headers?
  remote_addr_header: 'X-Forwarded-For',

  logging: {
    //add response-time http header
    response_time: true,
    //0: log all, 1: errors only, 2: warnings, 3: trace
    verbosity: 2
  },

  //template Engine
  template: {
    engine: 'jinja',
    defaults: {
      date_format: 'dS mmm yyyy h:MMtt'
    }
  }
});