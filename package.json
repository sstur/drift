{
  "name": "drift",
  "version": "0.1.10",
  "description": "Cross-platform Server JS",
  "main": "lib/server.js",
  "directories": {
    "test": "tests"
  },
  "bin": {
    "drift": "bin/drift"
  },
  "dependencies": {
    "assembler": "~0.1.3",
    "db-migrate": "~0.4.2",
    "optimist": "^0.6.1",
    "shelljs": "~0.2.6"
  },
  "devDependencies": {},
  "scripts": {
    "test": "mocha test",
    "install": "pushd node_modules && ln -s ../modules/drift-server && pushd drift-server && npm install && popd && ln -s ../modules/drift-utils && pushd drift-utils && npm install && popd && popd"
  },
  "_dependencies": {
    "drift-server": "file:modules/drift-server",
    "drift-utils": "file:modules/drift-utils"
  },
  "keywords": [
    "javascript",
    "fibers"
  ],
  "repository": {
    "type": "git",
    "url": "git@bitbucket.org:sstur/drift.git"
  },
  "author": "sstur",
  "license": "BSD-2-Clause"
}
