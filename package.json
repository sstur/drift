{
  "name": "drift",
  "version": "0.1.12",
  "description": "Cross-platform Server JS",
  "main": "lib/server.js",
  "directories": {
    "test": "tests"
  },
  "bin": {
    "drift": "bin/drift"
  },
  "dependencies": {
    "assembler": "~0.1.3",
    "db-migrate": "~0.4.2",
    "optimist": "^0.6.1",
    "shelljs": "~0.2.6"
  },
  "devDependencies": {},
  "scripts": {
    "test": "mocha test",
    "install": "cd node_modules && ln -s ../modules/drift-server && cd drift-server && npm install && cd .. && ln -s ../modules/drift-utils && cd drift-utils && npm install && cd ../.."
  },
  "_dependencies": {
    "drift-server": "file:modules/drift-server",
    "drift-utils": "file:modules/drift-utils"
  },
  "keywords": [
    "javascript",
    "fibers"
  ],
  "repository": {
    "type": "git",
    "url": "git@bitbucket.org:sstur/drift.git"
  },
  "author": "sstur",
  "license": "BSD-2-Clause"
}
