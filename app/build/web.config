<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <system.webServer>
    <defaultDocument>
      <files>
        <clear />
      </files>
    </defaultDocument>
    <httpProtocol>
      <customHeaders>
        <clear />
        <add name="Cache-Control" value="no-transform" />
      </customHeaders>
    </httpProtocol>
    <httpErrors errorMode="Detailed">
      <remove statusCode="500" subStatusCode="100" />
      <error statusCode="500" subStatusCode="100" path="/app/build/err.asp" responseMode="ExecuteURL" />
    </httpErrors>
    <rewrite>
      <rules>
        <rule name="assets" stopProcessing="true">
          <match url="^assets/" />
          <conditions>
            <add input="{REQUEST_FILENAME}" matchType="IsFile" />
          </conditions>
          <action type="Rewrite" url="{REQUEST_URI}" />
        </rule>
        <rule name="dispatch">
          <action type="Rewrite" url="app/build/app.asp" />
        </rule>
      </rules>
    </rewrite>
  </system.webServer>
</configuration>
