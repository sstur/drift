/*global app, define */
app.on('ready', function() {
  'use strict';

//  var Parser = require('html-parser');
//  var expect = require('expect');
//
//  //var fs = require('fs');
//  //var html = fs.readTextFile('/views/sample.html');
//  //var fragment = Parser.Fragment.fromHtml(html);
//
//  app.addTestSuite('html-parser', {
//    description: 'HTML Parser',
//    'basic parsing': function() {
//      var output = [];
//      var fragment = Parser.Fragment.fromHtml('<b>a>b</b>');
//      expect(fragment.children[0].children[0].value).to.be('a>b');
//      expect(fragment.getHtml()).to.be('<b>a>b</b>');
//    }
//  });

});
