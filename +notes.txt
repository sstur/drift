drift build wsh && cp build/app.wsf /Volumes/inetpub/wwwroot/sendfleet.com/local/build/repl.wsf
kill -s SIGHUP `cat server.pid`

cli:
  don't mutate argv._
  pass args instead of accessing argv global

